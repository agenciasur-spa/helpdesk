<!-- src/components/ui/Card.astro -->
---
const { title ="title", text ="text", btnUrl = "#" , typeCard = 1, btnIco="la", btnText="Ver más", col="col-md-3" } = Astro.props;

const bgClass = typeCard == 2 ? 'bg-info' : 'bg-danger';

---

<div class={col}>
  <div class="card h-100">
    <div class="card-body d-flex flex-column">
      <h5 class="card-title">{title}</h5>
      <p class="card-text flex-grow-1">{text}</p>
      <a href={btnUrl} class="btn btn-primary mt-auto">{btnText}</a>
    </div>
  </div>
</div>